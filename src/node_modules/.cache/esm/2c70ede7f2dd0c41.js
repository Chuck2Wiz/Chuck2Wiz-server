let mongoose,express,authRouter,morgan;_f2d‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_f2d‍.w("express",[["default",["express"],function(v){express=v}]]);_f2d‍.w("./routes/auth",[["default",["authRouter"],function(v){authRouter=v}]]);_f2d‍.w("morgan",[["default",["morgan"],function(v){morgan=v}]]);require('dotenv').config();





const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI)
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch((e) => {
    _f2d‍.g.console.error(e);
  });

const app = express();

app.set('port', PORT || 8001);

app.use(morgan('dev'));
app.use(express.json());

app.use('/api/v1/auth', authRouter);

app.listen(app.get('port'), () => {
  _f2d‍.g.console.log(app.get('port'), '번 포트에서 대기 중');
});
